/*
 * TenantSettings Messages
 *
 * This contains all the text for the TenantSettings component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.TenantSettings.header',
    defaultMessage: 'This is TenantSettings container !',
  },
});
